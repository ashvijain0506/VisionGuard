# Models

Saved trained model artifacts will be stored here when the training pipeline is complete.

Model experiments will focus on:

- A simple baseline
- Random Forest
- XGBoost

Model selection will be based on validation performance and interpretability rather than accuracy alone.
